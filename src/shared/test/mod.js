import {
  afterAll,
  afterEach,
  beforeAll,
  beforeEach,
  describe,
  it,
} from "@std/testing/bdd";

const test = {
  describe,
  it,
  beforeAll,
  afterAll,
  beforeEach,
  afterEach,
};

export default test;
